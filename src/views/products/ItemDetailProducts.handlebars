<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Detalles de Producto</title>
</head>
<body>
  <div class="container">
    <h1 class="my-3 text-center">Detalle del producto</h1>
    <div class="row mt-4">
      <div class="col-md-6 offset-md-3">
        <div class="card mb-4">
          <div class="card-body">
            {{#if message}}
              <h5 class="card-title">Titulo:</h5>
              <p class="card-text">{{message.title}}</p>
              <h5 class="card-title">Descripción:</h5>
              <p class="card-text">{{message.description}}</p>
              <h5 class="card-title">Precio:</h5>
              <p class="card-text"> ${{message.price}}</p>
              <h5 class="card-title">Stock:</h5>
              <p class="card-text">{{message.stock}}</p>
              <h5 class="card-title">Categoría:</h5>
              <p class="card-text">{{message.category}}</p>
              <h5 class="card-title">Código:</h5>
              <p class="card-text">{{message.code}}</p>
              <h5 class="card-title">Status:</h5>
              <p class="card-text">{{message.status}}</p>

              <div class="d-flex justify-content-between mt-3">
                <a href="/api/products" class="btn btn-primary">Volver a la Lista</a>
                <a href="#" class="btn btn-primary">Agregar al Carrito</a>
              </div>

            {{else}}
              <p>No se encontró el producto.</p>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
